<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.user.findAccount">
	<!-- 아이디 조회 -->
	<select id="selectId" parameterType="kr.co.sist.user.findAccount.FindAccountDTO" resultType="String">
		select  user_id
		from 	member
		where 	name_hash = #{nameHash} and email_hash = #{emailHash}
	</select>
	
	<!-- 회원 정보 조회 -->
	<select id="selectMember" parameterType="kr.co.sist.user.findAccount.FindAccountDTO" resultType="int">
		select 	count(*) cnt
		from 	member
		where 	user_id = #{id} and name_hash = #{nameHash} and email_hash = #{emailHash}
	</select>
	
	<!-- 임시 비밀번호 업데이트 -->
	<update id="updateMember" parameterType="kr.co.sist.user.findAccount.FindAccountDTO">
		update	member
		set 	user_pass = #{tempPass}
		where	user_id = #{id}
	</update>
</mapper>









