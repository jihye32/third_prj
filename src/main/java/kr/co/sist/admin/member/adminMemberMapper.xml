<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.admin.member">

    <select id="selectTotalCount" resultType="int" parameterType="kr.co.sist.admin.member.AdminMemberDTO">
        SELECT COUNT(*) FROM member
        <where>
            <if test="keyword != null and keyword != ''">
                AND userId LIKE '%' || #{keyword} || '%'
            </if>
        </where>
    </select>

    <select id="selectMemberList" resultType="kr.co.sist.admin.member.AdminMemberDomain" parameterType="kr.co.sist.admin.member.AdminMemberDTO">
        SELECT *
        FROM (
            SELECT rownum rnum, m.* FROM (
                SELECT 
                    memberNo, userId, email, inputDate
                FROM member
                <where>
                    <if test="keyword != null and keyword != ''">
                        AND userId LIKE '%' || #{keyword} || '%'
                    </if>
                </where>
                ORDER BY inputDate DESC
            ) m
        )
        WHERE rnum BETWEEN #{startNum} AND #{endNum}
    </select>

</mapper>